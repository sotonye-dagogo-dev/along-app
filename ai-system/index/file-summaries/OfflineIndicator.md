# OfflineIndicator

**Path:** `app/components/ui/OfflineIndicator.tsx`

**Purpose:** 'use client' component that listens to `OnlineStatusProvider` and renders a full-screen overlay with an `AppEmptyState` preset when offline. Returns `null` when online.

**Key exports:** `OfflineIndicator` (default)
