# AppLogo

**Path:** `app/components/ui/AppLogo.tsx`

**Purpose:** Renders the Along branded SVG logo with optional text wordmark, configurable size (`sm`/`md`/`lg`/`hero`), and optional link wrapper. Draws from `LOGO_CONFIG` registry.

**Key exports:** `AppLogo` (default)
